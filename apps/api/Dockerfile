# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @nx-bugs/api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t nx-bugs-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system nx-bugs-api && \
          adduser --system -G nx-bugs-api nx-bugs-api

COPY apps/api/dist nx-bugs-api/
COPY apps/api/package.json nx-bugs-api/
RUN chown -R nx-bugs-api:nx-bugs-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix nx-bugs-api --omit=dev -f install

CMD [ "node", "nx-bugs-api" ]
